# Implementation Plans

Plans 001-010 generated by the improve skill on 2026-07-17 against commit `7ce587e`, from a four-agent adversarial audit in which every finding was re-verified against the code by the advisor before planning. Plans 011-015 generated the same day by the improve-react skill (React/Preact-focused pass: React Doctor scan + four category auditors, every finding re-verified at its cited lines) against the same commit. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Verification gate for every plan (also inside each file): `bun install --frozen-lockfile`, then `bun run typecheck && bun run lint && bun run format:check && bun run test` — all exit 0. CI runs this same gate plus `bun run build` and `bun run e2e`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Flag the DOM driver's synthetic Escape | P1 | S | — | DONE |
| 002 | Gate input listeners on `Event.isTrusted` | P1 | S | 001 | TODO |
| 003 | Make assign runs single-flight | P1 | S | — | TODO |
| 004 | Match Lists dialog rows by exact name | P1 | S | 001 | TODO |
| 005 | Bind toast Undo buttons to their armed action | P1 | M | 003 | TODO |
| 006 | Lazy GraphQL creds, unbrickable boot, real `getLists` | P1 | S | — | TODO |
| 007 | Move settings to storage.local + migration | P1 | S | — | TODO |
| 008 | Keep assign Undo visible for the full window | P2 | S | — | TODO |
| 009 | Report unmute failures with the right verb | P2 | S | 005 | TODO |
| 010 | Selection-store merge keeps resolved fields | P2 | S | — | TODO |
| 011 | Tear down per-tweet overlay roots on article removal | P1 | M | — | TODO |
| 012 | Scope overlay subscriptions to their own selected state | P1 | S-M | 010, 011 | TODO |
| 013 | Select-mode clicks resolve the outermost article | P1 | S | 011 | TODO |
| 014 | Picker keyboard contract: imperative focus + combobox ARIA | P1 | M | — | TODO |
| 015 | Pre-mounted toast live regions | P1 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **002 after 001**: both concern synthetic events; 001's regression test asserts the flag is set, independent of the trust gate, but landing 001 first keeps the story linear. After 002, the flag in 001 becomes defense-in-depth (documented in both plans).
- **004 after 001**: same file (`src/core/x-client/dom-page-driver.ts`) — avoids a merge collision.
- **005 after 003**: both touch `src/content/controller.ts`; 003 is smaller, land it first.
- **009 after 005**: same file (`src/content/controller.ts`), same `unmuteAuthor` region.
- 006, 007, 008, 010 are independent of everything — they can be picked in parallel with any other plan.
- **012 after 010**: both edit `src/core/selection-store.ts`, and 012's size-neutral-merge test is the regression test that makes 010's fix user-visible — without 012, fields merged by 010 never notify the UI (the `count`-as-change-proxy gap, documented in 012's Problem section). **010 executor: read that section before starting.**
- **012 after 011, 013 after 011**: 011 restructures the scanner wiring in `src/content/main.tsx` and the overlay lifecycle; 012 (app.tsx subscriptions) and 013 (main.tsx click handler) assume 011's shape. 013 also collides with 002's region — keep the 001→002 order, then 013.
- 014 and 015 are independent of everything (each touches one `src/ui/` file no other plan edits).

## Findings considered and rejected

(Recorded so nobody re-audits them. "Deferred" = real finding, not planned in this batch; "not a finding" = vetted and dismissed.)

- **Public web bearer committed in `src/core/x-client/auth.ts:7`** — not a finding: it is X's well-known public web-client token, by design per ADR-0002.
- **Human-paced sleeps/jitter as a performance issue** — not a finding: deliberate ADR-0005 policy invariant.
- **MAIN-world bridge spoofability as a vulnerability** — not a finding at vulnerability grade: the bridge holds no tokens or privileged APIs and grants no authority the page doesn't already have. Kept as deferred hardening (target allowlist in `src/content/main-world.ts`).
- **`dist/`, `coverage/`, `test-results/` committed to git** — not a finding: verified gitignored, zero tracked files.
- **`@resvg/resvg-js`** — not a finding: dev-only (icons build script).
- **`bun audit`** — clean at audit time.
- **GraphQL backend's private-endpoint use** — not a finding: documented user opt-in (ADR-0005/0007).

## Deferred findings (vetted, not planned in this batch)

Tier 2/3 findings from the audit, deliberately not planned now. Each has evidence in the audit session; re-run `/improve` to plan any of them.

- **Lists pagination cursors ignored** (`lists-provider.ts:33,70-84`) — >100 Lists silently vanish from the picker. M effort, MED confidence on page size.
- **Cold picker open double-fetch** (`picker-controller.ts:106-124`) — redundant second `ownerships` request. S.
- **Every overlay re-renders on any selection toggle** (`app.tsx:42`) — O(timeline) renders per click. S-M.
- **Coach storage IPC per hover** (`app.tsx:47-59`, `coach.ts`) — 2-3 storage round-trips per post hover, forever. S.
- **Layout thrash in observer callback** (`main.tsx:67-69`) — getComputedStyle read interleaved with writes. S, MED.
- **REST backend missing from the shared contract test** (`tests/core/x-client/contract.test.ts:62-65`) — ADR-0007 claims all three share it; also `removeMember` has no contract. S.
- **100% coverage gate never enforced** (`vitest.config.ts:21` vs `package.json:12`, CI) — thresholds configured but never run in CI. M.
- **`main.tsx`/`app.tsx`/`main-world.ts` zero unit coverage** — the accepted-click machinery is fixture-e2e-only. M.
- **E2E fixture-vs-live drift unowned** (`e2e/content.spec.ts:67-88`) — needs a re-verification cadence. M.
- **Auth headers + error classification triplicated with drift** (`rest-api.ts`, `graphql-api.ts`, `lists-provider.ts`) — GraphQL copy drops `resetAt` and diverges on codes. S-M.
- **Dead code batch** — unwired GraphQL sniffer (ADR-0004 decision needed: wire or delete), `list-usage.rank`, `list-cache.search`, `@preact/signals` dep, `hotkeySelectMode` field. S-M.
- **Doc drift batch** — CONTEXT.md module map (wrong default backend, phantom handle→id cache), `graphql-config.ts:11` comment, `unit-test-design.md:3` status line. S.
- **Scanner-health false positives on tweet-less pages** (`scanner-health.ts:23-30` + all-pages content script match). S, MED.
- **Unpaced undo removals** (`controller.ts:119-130`) — undoing a large run bursts `members/destroy` calls; ADR-0005 silent on removals. S, MED.
- **WAR chunk exposure** (`dist/manifest.json:54-68`) — 4 extension chunks fetchable by x.com; surface reduction. S-M, MED.
- **MAIN-world bridge target allowlist** (`main-world.ts:59-103`) — hardening, low ceiling. S.
- **Investigate-grade** — main-world 250 ms ack double-click under jank (`caret-actions.ts:66`); scanner node-reuse stale overlay (`tweet-scanner.ts:28-35`).
- **DX batch** — no README/`check` script, no root AGENTS.md, root `verify-*.md` sprawl, crxjs declared beta vs locked stable (`package.json:30` vs `bun.lock:81`).

## React pass addendum (improve-react, 2026-07-17, commit `7ce587e`)

React Doctor 0.7.8 scan: score 62, 128 diagnostics — **117 rejected as scanner noise** (`no-unknown-property` fired on Preact's idiomatic `class` attribute despite the scanner detecting Preact 10; the score is meaningless until that rule is gated upstream). Of the remaining 11: the two modal `prefer-html-dialog` hits are real (deferred below), the two palette/popover hits are documented deviations (see plan 014), and the 5 micro-perf hits are cold-path noise. The 2 `async-await-in-loop` hits (controller.ts:123, caret-actions.ts:292) are rejected: sequencing there is deliberate (ADR-0005 pacing / DOM step ordering) — `Promise.all` would make burst behavior worse.

### Deferred (vetted this pass, not planned in this batch)

- **Coach storage cluster** (`coach.ts:50-54,75-87` + `controller.ts:156,181` + `app.tsx:47-59` + `ActionBar.tsx:100-101`) — three related defects worth one plan: (a) non-atomic read-merge-write on the single coach key races the unawaited `recordAssign` against `tryShowTip` on every pointer assign — last writer wins, losing either the decay counter or the once-only tip; (b) `tryShowTip` consumes the tip in storage before the caller knows it can still display — a sub-roundtrip hover permanently burns the one-time "first-hover" tip unseen, and ActionBar's count tooltip has no staleness guard at all (can appear after mouse-leave and stick); (c) 3-4 storage IPC round-trips per post-hover, ≥1 forever after decay (already in the deferred list above). M effort.
- **No Preact error boundary** (zero `componentDidCatch` in src) — a render throw in a per-tweet overlay propagates out of a signal notification; in the select-mode click path this happens AFTER `preventDefault`+`stopImmediatePropagation` (`main.tsx:218-220`), so the user's click on a post is silently swallowed. Wrap App root + overlay roots. S-M.
- **Picker background-refresh race** (`picker-controller.ts:112-120`) — the silent `force:true` refresh can swap/shrink/reorder the list mid-keyboard-navigation; plan 014 makes highlight and Enter agree, but the underlying mid-navigation swap (freeze list while open, or reconcile activeIndex by id) remains. S-M.
- **Modal focus traps** (`WelcomeCard.tsx:19-23`, `ShortcutsSheet.tsx:32-36`; rule `react-doctor/prefer-html-dialog` — real here) — `aria-modal="true"` with no focus move-in/trap/restore; native `<dialog>.showModal()` is the canonical fix for these two true modals. S-M.
- **Selection loop has no SR feedback** (`main.tsx:207-223`, `ActionBar.tsx:40-58,98-112`) — selects/deselects and run progress ("Adding 3 of 12…") announce nothing; builds on 015's live regions. S-M.
- **Focus restoration on close** (picker/review/sheets — Esc flips signals, focus stranded), **TweetOverlay keyboard reveal** (hover-only, `tabIndex=-1` when hidden), **single-key shortcuts not remappable/disableable** (WCAG 2.1.4), **listbox group semantics**, **tooltip pattern (1.4.13)**, **options-page confirmations not announced**, **hc-mode under-delivery** (`styles.css:45-47` only re-tints accent), **overlay-host-inside-avatar-anchor nesting** (verify live). S each.
- **Default accent contrast is a product call, not a plan**: `--color-accent oklch(0.68 0.15 237)` + white ≈3:1 on the primary CTA/success toasts (WCAG 1.4.3 fail); the AA-safe fill exists but is opt-in behind `data-hc`. Blending into X's own (also-failing) blue is documented intent (`styles.css:6-8`) — decide deliberately: make the darker fill default, or record the tradeoff in an ADR.
- **Stale settings on on-demand wake** (`main.tsx:259,269`) — `start()` uses the boot-time settings snapshot; `settingsStore.subscribe` exists and is never wired. S.
- **Main-world handshake duplication** (`main-world.ts:1-55` vs `caret-actions.ts:15-19,75-127`) — the five PAGE_ACTIVATE_* channel constants + pointer recipe are hand-duplicated across worlds with no shared module or pinning test; a drift is masked by the 250 ms ack fallback as a plausible double-toggle. S.
- **twitter.com injection is a dead path** (`manifest.config.ts:6,32` vs x.com-hardcoded API origins in `rest-api.ts:6`, `lists-provider.ts:33,75`, `graphql-config.ts:15`) — extra origin exposure for zero benefit; every fetch would die cross-origin. S.
- **Dead-code additions** (beyond the batch above): `CaretActions.mute/.block` + `DriverSelectors.BLOCK` unreachable (REST wired instead, `main.tsx:160-162`); `"block"` CommandId has no binding or UI ("destructive, opt-in" per `keyboard.ts:22-24`, but no opt-in mechanism exists); cross-context message types are scattered inline literals with inconsistent naming (`lasso:badge` vs `lasso-activate`); `use-signal-value.ts:5-7` comment misstates why `@preact/signals` isn't used (the adapter is runtime-based, not babel-dependent) — fix the comment before deciding the keep-or-drop on the unused dep. S.

### Verified clean this pass

Zero HTML-injection sinks (no `innerHTML`/`dangerouslySetInnerHTML` in src; page-derived strings render as JSX text or go through `URLSearchParams`); background `onMessage` is same-extension + `sender.tab`-gated with re-validated fields; credentials never logged/persisted; `use-signal-value.ts` has no subscribe-gap bug (signals-core `subscribe` fires synchronously with the current value); toast/undo timers are cleaned up; dormant mode is genuinely inert; `controller.ts` at 367 lines is cohesive and fully tested — no split warranted.
