# Mission

Learn to debug browser-extension interactions against live X carefully enough that a green local test means the real user workflow is likely to work.

The practical goal is to avoid repeating failed fixes where Lasso reports success while X ignores the action. A valid fix must prove the external site accepted the click, not merely that our code dispatched an event.

